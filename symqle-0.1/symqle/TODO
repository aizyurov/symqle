(done) adaptive DialectDatasource
(done) rename DialectDatasource -> DataSourceGate
(done) common functions
database-specific functions
(done) Remove singletons -use no-arg constructor for dialects.
(done) decouple dialect mappings and DataSourceGate (via symqle-dialect.properties).

Postgresql dialect
Oracle dialect
Sybase dialect
HSQL dialect (2.3.1) ?

test transactions
SpringEngine (transactions)

SmartSelector: get(MapRow); map-like implementation of Row - key is Sql produced with DebugDialect/MockTableRegistry.
MockTableRegistry returns table.toString for getCorrelationName (so subquiries beleive all tables are from top level scope).
