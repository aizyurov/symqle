(done) adaptive DialectDatasource
(done) rename DialectDatasource -> DataSourceGate
(done) common functions
database-specific functions
(done) Remove singletons -use no-arg constructor for dialects.
(done) decouple dialect mappings and DataSourceGate (via symqle-dialect.properties).

(done) Postgresql dialect
Oracle dialect
Sybase dialect
HSQL dialect (2.3.1) ?
H2 dialect

test transactions
SpringEngine (transactions)

(done) SmartSelector: get(MapRow); map-like implementation of Row - key is Sql produced with DebugDialect/MockTableRegistry.
MockTableRegistry returns table.toString for getCorrelationName (so subqueries beleive all tables are from top level scope).

(done) CompiledStatement CompiledUpdate extends Sql, CompiledQuery extends Query
formatting inside; Engine accepts CompiledUpdate etc.
SelectStatement#compile(Engine, Option...)

(done different way) refactoring of DynamicParameter: make abstract; ConstantParameter and MutableParameter.
static DynamicParameter.constant(Mapper<T>,T) and DynamicParameter.mutable(Mapper>T>).
resp. Params.p() and Params.mutableXxx();