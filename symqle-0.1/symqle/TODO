(done) adaptive DialectDatasource
(done) rename DialectDatasource -> DataSourceGate
(done) common functions
database-specific functions
decouple dialect mappings and DataSourceGate (via symqle-dialect.properties). 
(done) Remove singletons -use no-arg constructor for dialects.

Postgresql dialect
Oracle dialect
Sybase dialect

test transactions
SpringDatabaseGate (transactions)
refactor DatabaseGate: executeQuery, executeUpdate, execute(ConnectionCallback) + BatchingGate: submitUpdate, flush.
move normalization of SQL to Dialect (may be different for different dialects)
